{
    "name": "backend",
    "version": "1.0.0",
    "type": "module",
    "private": true,
    "scripts": {
      "predev":"npm run build --prefix ../shared",
      "dev":"tsx watch src/server.ts",
      "prebuild": "npm run build --prefix ../shared",
      "build": "tsc",
      "start": "node dist/server.js"

    },
    "dependencies": {
      "@news-portal/shared": "*",
      "express": "^4.19.0"
    },
    "devDependencies": {
      "typescript": "^5.5.0",
      "tsx": "^2.0.0",
      "@types/express": "^4.17.21",
      "@types/node": "^20.14.0"
    }
  }